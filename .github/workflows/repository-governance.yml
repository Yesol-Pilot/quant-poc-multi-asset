name: Repository Governance

on:
  pull_request:
  push:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  governance:
    uses: Yesol-Pilot/neo-evidence-gate/.github/workflows/repository-governance-reusable.yml@main
